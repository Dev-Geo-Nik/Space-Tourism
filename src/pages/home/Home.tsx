import styles from './home.module.scss';

const  Home :React.FC = () => {

  return (
<div className={`container  flow`}>
    <h1>Design system</h1>
    <section id="colors">x</section>
</div>
);
};

export default Home;

